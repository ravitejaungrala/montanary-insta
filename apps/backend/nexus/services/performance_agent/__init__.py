"""Performance Agent — ranks outreach patterns (channel, message variant,
audience segment, timing) by outcome, so winning combinations can be reused
in future campaigns instead of guessed at cold each time.

See /implementation.md at the repo root for the full design doc.
"""
